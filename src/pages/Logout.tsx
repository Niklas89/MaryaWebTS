import { useEffect } from "react";
import useLogout from "../hooks/useLogout";

const Logout = () => {
    useEffect(() => {
        useLogout
    })
    return (
        <div>
            
        </div>
    );
};

export default Logout;